# Auth module

Owns registration, login, logout, current-user lookup, password hashing, JWT issuance, and safe user DTO mapping. Implement in a focused `feat/api-auth` branch; no notes code belongs here.
